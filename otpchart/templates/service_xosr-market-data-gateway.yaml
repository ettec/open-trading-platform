apiVersion: v1
kind: Service
metadata:
  labels:
    app: market-data-source
    mic: XOSR
  name: xosr-market-data-gateway
spec:
  externalTrafficPolicy: Cluster
  ports:
  - name: api
    port: 50551
    protocol: TCP
    targetPort: 50551
  selector:
    app: xosr-market-data-gateway
  sessionAffinity: None
  type: NodePort
