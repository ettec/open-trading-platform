syntax = "proto3";
package marketdataservice;
import "modelcommon.proto";

option go_package="api";

message ListingId {
    int32 listingId = 1;
}

service BookBuilderStrategy {
    rpc Start(ListingId) returns (model.Empty) {};
    rpc Stop(ListingId) returns (model.Empty) {};
}